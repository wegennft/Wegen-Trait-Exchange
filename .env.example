# Copy to .env and fill in values (or pull from Replit Secrets)
DATABASE_URL=postgresql://localhost:5432/wegen_trait_store
SESSION_SECRET=change-me-to-a-random-string
NODE_ENV=development

# API server (Terminal 1)
# PORT=8080

# Frontend (Vite — also create artifacts/wegen-trait-store/.env)
# PORT=22211
# BASE_PATH=/
# VITE_WALLETCONNECT_PROJECT_ID=your-reown-project-id
