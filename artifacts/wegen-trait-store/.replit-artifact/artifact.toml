kind = "web"
previewPath = "/"
title = "Wegen NFT Trait Store"
version = "1.0.0"
id = "artifacts/wegen-trait-store"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22211

[services.development]
run = "pnpm --filter @workspace/wegen-trait-store run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/wegen-trait-store", "run", "build" ]
publicDir = "artifacts/wegen-trait-store/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22211"
BASE_PATH = "/"
