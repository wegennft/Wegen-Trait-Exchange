I want to start a fresh build of this app on another platform, but I want to keep the same concept, user flows, features, pages, and functionality.

Please study this Replit project and create a complete rebuild blueprint for me.

I need:

1. A plain-English summary of what this app does
   Explain the purpose of the app, who it is for, and what problem it solves.

2. A complete feature list
   List every feature that currently exists, including pages, dashboards, forms, buttons, user actions, admin actions, database actions, uploads, API calls, automations, notifications, payment flows, login/signup, and anything happening in the background.

3. A page-by-page breakdown
   For each page or route, tell me:

* The route URL
* What the page is for
* What users can do there
* What buttons or actions exist
* What data is shown
* What data is created, edited, or deleted
* What should happen after each user action

4. A user-flow breakdown
   Explain the full journey for each type of user. Include things like:

* New visitor
* Logged-in user
* Admin
* Seller
* Buyer
* Creator
* Any other role this app uses

5. A database/schema breakdown
   Explain all tables, collections, fields, relationships, example records, and what each one is used for. Also explain what data needs to be saved for each feature to work.

6. Backend/API breakdown
   List every backend route, server action, function, or API call. Explain what each one does, what input it needs, what it returns, and which feature uses it.

7. Environment variables and outside services
   List every outside service this app depends on, such as databases, auth providers, APIs, payment systems, storage, email tools, AI tools, or webhooks. List the needed environment variable names with placeholder values only. Do not reveal secrets.

8. Design and UI notes
   Describe the visual style, layout, colors, components, buttons, cards, modals, dashboards, mobile behavior, and any important design details that should be recreated.

9. Current issues and broken parts
   List anything that is malfunctioning, incomplete, confusing, duplicated, insecure, or not working as intended. For each issue, explain:

* Where it happens
* What the current problem is
* What the correct behavior should be

10. Fresh build plan
    Give me the best order to rebuild this app from scratch on another platform. Break it into phases:

* Foundation
* Database
* Authentication
* Core pages
* Core features
* Admin tools
* Testing
* Launch polish

11. Copy-ready prompt for the next AI builder
    At the end, write a detailed prompt I can paste into another AI coding tool so it can rebuild the app from scratch using this blueprint.

Please be extremely detailed and do not assume the next builder can see this Replit project. The goal is to create a rebuild blueprint clear enough that another platform can recreate the app with the same functionality, but cleaner and more stable.
